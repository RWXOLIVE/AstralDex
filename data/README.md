# data

This folder is reserved for data files generated by `porydex` for a Showdown dex.

`item-location-notes.js` is the manual exception. Edit that file to add or override
the bracketed note text shown on item location rows.
